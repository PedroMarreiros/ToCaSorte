<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tocasorte">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".TotoLotoView"></activity>
        <activity android:name=".TotoLotoInsert" />
        <activity android:name=".EuroMillionView" />
        <activity android:name=".EuromillionInsert" />
        <activity android:name=".TotoBolaView" />
        <activity android:name=".TotoBolaInsert" />
        <activity android:name=".MiniGame" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".TotolotoActivity" />
        <activity android:name=".TotobolaActivity" />
        <activity android:name=".EuromilionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>